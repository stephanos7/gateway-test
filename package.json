{
  "name": "tyk-mock-api",
  "version": "1.0.0",
  "description": "an api to test the tyk gateway",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --exec babel-node --",
    "start-dev": "nodemon index.js --exec babel-node --",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "pg": "^8.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
